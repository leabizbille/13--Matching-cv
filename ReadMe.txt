python --version

pip install -r requirements.txt

where python    # Version python et localisation sur ordi.
#-----------------------------------------------------------------
python -m venv myenv
myenv\Scripts\activate       #Création d'un environnement : 

pip install -r requirements.txt
pip install --upgrade pip
